<div class="flex flex-col items-center  min-h-screen bg-gray-100">
    <div class="bg-slate-50 rounded-lg p-6 mb-4 border border-slate-200 w-full max-w-2xl min-h-screen">
        <h2 class="text-2xl font-bold mb-4 text-center"><%= @review.title %></h2>
        <p class="text-gray-700 mb-2"><strong>ジャンル:</strong> <%= @review.genre %></p>
        <%= @review.title %>
        <%=@review.genre %>
    </div>

</div>